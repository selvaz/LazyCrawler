# LazyCrawler — dependencies
#
# Core (PURE mode, no LLM):
requests>=2.28
beautifulsoup4>=4.11
trafilatura>=1.6
pydantic>=2.0

# SMART mode (LLM via LazyBridge) — also install the provider extra:
#   pip install "lazybridge[anthropic]"  (or openai / google / deepseek)
lazybridge>=0.9

# PDF (remote extraction):
pymupdf>=1.23
pypdf>=4.0
pdfplumber>=0.10

# WebSearch (DuckDuckGo):
ddgs>=2.0

# JavaScript rendering (optional, for HTTPConfig(render_js=True)):
#   pip install playwright && playwright install chromium
playwright>=1.40

# Blacklist from Excel:
openpyxl>=3.1

# Robust date parsing:
python-dateutil>=2.8
