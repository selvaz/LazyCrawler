# LazyCrawler — dependencies
#
# This file is a convenience pointer at pyproject.toml so the two never drift.
# It installs LazyCrawler with ALL optional features (equivalent to the [all]
# extra), including the correct environment markers (e.g. LazyBridge is only
# pulled in on Python >= 3.11).
#
#   pip install -r requirements.txt
#
# For a minimal PURE-mode install (no LLM, no extras):  pip install -e .
# For a specific feature set:                           pip install -e ".[ml,pdf,async]"
#
-e .[all]
