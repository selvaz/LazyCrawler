# LazyCrawler — dipendenze
#
# Core (PURE mode, nessun LLM):
requests>=2.28
beautifulsoup4>=4.11
trafilatura>=1.6
pydantic>=2.0

# SMART mode (LLM via LazyBridge) — installa anche l'extra del provider:
#   pip install "lazybridge[anthropic]"  (o openai / google / deepseek)
lazybridge>=0.9

# PDF (estrazione remota):
pymupdf>=1.23
pypdf>=4.0
pdfplumber>=0.10

# WebSearch (DuckDuckGo):
ddgs>=2.0

# Blacklist da Excel:
openpyxl>=3.1

# Parsing date robusto:
python-dateutil>=2.8
